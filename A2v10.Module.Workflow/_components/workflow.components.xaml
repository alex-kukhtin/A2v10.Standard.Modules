<ComponentDictionary xmlns="clr-namespace:A2v10.Xaml;assembly=A2v10.Xaml"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
	<InlineDialog x:Key="ProcessArguments" Id="args" Title="Workflow Arguments" Width="20rem" Padding="0,0,2rem,0">
		<InlineDialog.Buttons>
			<Button Style="Primary" Content="@[Ok]" Command="{BindCmdExec startWorkflow, Argument={Bind}}"/>
			<Button Content="@[Close]" Command="{BindCmd Close}"/>
		</InlineDialog.Buttons>
		<Slot Scope="{Bind}">
			<Grid Height="100%" Columns="1*, Auto" AlignItems="Bottom">
				<Repeater ItemsSource="{Bind Arguments}">
					<Group>
						<TextBox Label="{Bind Name}" Value="{Bind Value}"/>
						<Label Content="{Bind Type}" Margin="14,0,0,0"/>
					</Group>
				</Repeater>
			</Grid>
		</Slot>
	</InlineDialog>
</ComponentDictionary>

